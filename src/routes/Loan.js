import React from 'react';
import Navbar from '../components/Navbar';

const Loan = () => (
  <div>
    <Navbar
      title="Compare loans"
      subTitle="What type of loan are you looking for?"
    />
    <p>Dashboard</p>
  </div>
);

export default Loan;
